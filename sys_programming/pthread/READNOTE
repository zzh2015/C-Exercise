asynchronous:事情相互独立地发生。
concurrency:实际上可能串行发生的事情好像同时发生一样
并行(parallenlism):并发序列同时执行,事情在相同的方向上独立进行。
    通常需要改变接口来使函数可重入。
    线程：PC，SP，通用寄存器，浮点或地址寄存器（协处理器状态和协处理器寄存器）。
        不包括地址空间、文件描述符。
        线程共享文件和内存空间，程序文本段和数据段。
